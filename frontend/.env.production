# Root-relative URLs (/static/js/..., /static/css/...) so asset requests always hit
# the real files at the site root — avoids resolving ./static relative to nested routes
# and matches servers that serve the SPA only from /.
PUBLIC_URL=/
